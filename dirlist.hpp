#ifndef __DLAUNCHER_DIRLIST_HPP__
#define __DLAUNCHER_DIRLIST_HPP__

#include <string>
#include <vector>

int dirlist(const std::string &dirname, std::vector<std::string> &r, const std::string &cache_file_prefix);

#endif
